"""
description: Create time series of the nbi bridges

idea:
    1. Specify the attributes that are required
    2. Specify state and years of the records required
    3. for the state and years:
        download_csv_files()
        open(downloaded_csv_file)
        extract(attributes) -> create_dictionary(structure_number, attributes)

        *create a separate mapping of structure number and all attributes over the years*

"""


